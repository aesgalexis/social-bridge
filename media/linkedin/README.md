# LinkedIn media

Store image files referenced by LinkedIn outbox requests in this directory.

Supported formats: JPG, PNG, and GIF.

Example request reference:

```json
{
  "image": {
    "path": "media/linkedin/example.png",
    "altText": "Short accessible description"
  }
}
```

The repository is public, so media committed here is public too.
